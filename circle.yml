general:
  branches:
    only:
      - master
      - release

machine:
  timezone:
    Asia/Tokyo
  node:
    version: 4.0.0

dependencies:
  pre:
    - export NODE_PATH=./

test:
  post:
    - npm test

deployment:
  staging:
    branch: develop
    commands:
      - npm run package
