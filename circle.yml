general:
  branches:
    only:
      - master
      - release

machine:
  timezone:
    Asia/Tokyo
  node:
    version: 4.0.0

test:
  post:
    - NODE_PATH=./ && npm test

deployment:
  staging:
    branch: release
    commands:
      - npm run package
