machine:
  timezone:
    Asia/Tokyo
  node:
    version: 4.0.0
  pre:
    - npm install
    - npm run init

test:
  post:
    - NODE_PATH=./ && npm test

deployment:
  production:
    branch: release
    commands:
      - npm run package
